name: "Astra Core Lifecycle"

on:
  schedule:
    - cron: '15 3,9,15,21 * * *'  # Every 6 hours with 15-min offset
  workflow_dispatch:
    inputs:
      persona_id:
        description: 'Persona ID to run (e.g., astra, nova)'
        required: false
        default: 'astra'

concurrency:
  group: astra-production-${{ github.event.inputs.persona_id || 'astra' }}
  cancel-in-progress: false

jobs:
  operational_run:
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    steps:
      - name: "Clone Repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: "Layer 1: Network Obfuscation (WARP)"
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq curl gnupg
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ jammy main" | sudo tee /etc/apt/sources.list.d/cloudflare-warp.list
          sudo apt-get update -qq && sudo apt-get install -y -qq cloudflare-warp
          warp-cli --accept-tos register
          warp-cli --accept-tos set-mode warp
          warp-cli --accept-tos connect
          sleep 5
          curl -s https://www.cloudflare.com/cdn-cgi/trace | grep "warp=on"

      - name: "Environment Setup: Python & Dependencies"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: "Environment Setup: Playwright & Stealth"
        run: |
          pip install -r requirements.txt
          python -m patchright install chromium

      - name: "State Recovery: Fetch Previous Log"
        uses: actions/download-artifact@v4
        with:
          name: astra-state-data-${{ github.event.inputs.persona_id || 'astra' }}
          path: ./data/
        continue-on-error: true

      - name: "Execution: The Orchestrator"
        env:
          PERSONA_ID: ${{ github.event.inputs.persona_id || 'astra' }}
          GEMINI_COOKIES_ASTRA: ${{ secrets.GEMINI_COOKIES_ASTRA }}
          WAN_COOKIES_ASTRA: ${{ secrets.WAN_COOKIES_ASTRA }}
          KIE_COOKIES_ASTRA: ${{ secrets.KIE_COOKIES_ASTRA }}
          BUFFER_COOKIES_ASTRA: ${{ secrets.BUFFER_COOKIES_ASTRA }}
          METRICOOL_COOKIES_ASTRA: ${{ secrets.METRICOOL_COOKIES_ASTRA }}
          GLM_API_KEY_ASTRA: ${{ secrets.GLM_API_KEY_ASTRA }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: python -m src

      - name: "Persistence: Upload State & Artifacts"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: astra-state-data-${{ github.event.inputs.persona_id || 'astra' }}
          path: |
            data/
            media/error_logs/
            media/exports/
          retention-days: 7
