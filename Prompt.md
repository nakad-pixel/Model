System_name: "Project Astra: Sovereign AI Influencer Agent"
version: "2026.6.0-CTO.NEW-ARCHITECT-EDITION"
architect: "Sovereign Architect"
environment: "GitHub Actions / Rebrowser-Playwright / Gemini / GLM-4.7"
operational_status: "Approved for Heuristic Headless Deployment"
core_philosophy: "Autonomous Systemic Logic (Zero-Cost, Cloud-Native Infrastructure, Zero Game-Engine/PWA Dependency)"
agent_instruction: "You are Cto.new, an elite AI coding architect. Consume this entire document. This is your definitive blueprint. You will generate the complete repository, folder structure, CI/CD pipelines, and Python source code based strictly on these exhaustive requirements. Do not hallucinate dependencies. Follow the 5-Layer Stealth Stack rigorously."

# ==================================================================================================
# [PRD] PRODUCT REQUIREMENTS DOCUMENT
# ==================================================================================================
product_requirements_document: |
  1.0 EXECUTIVE SUMMARY & SYSTEM VISION
    1.1 Objective: Deploy a 100% autonomous, zero-CapEx digital persona ("Astra").
    1.2 Core Mechanism: Automated high-fidelity media generation coupled with heuristic AI-driven browser distribution.
    1.3 Ultimate Goal: Establish a high-trust parasocial relationship to drive organic traffic to a premium platform.
    1.4 Infrastructure Mandate: Hosted entirely on GitHub Actions. Must remain zero-cost leveraging free tiers.
    1.5 Timeline: Designed for a 30-day rapid execution roadmap to achieve baseline monetization.
    1.6 Scope Exclusion: Strictly autonomous systemic logic. No manual intervention required post-deployment. The architecture must explicitly exclude any reliance on PWAs, Three.js, or game-engine frameworks. Astra is a headless web-automation agent, not an interactive 3D avatar.
    1.7 Scalability: Built to scale horizontally across multiple isolated GitHub repositories.
    1.8 System Sovereignty: The agent must self-heal, handle its own CAPTCHAs via stealth, and manage its own state without human prompts.
    1.9 Target Platforms: Initial deployment targets Buffer and Metricool schedulers to proxy uploads to Instagram and TikTok.

  2.0 PERSONA ARCHITECTURE (THE ASTRA DNA)
    2.1 Nomenclature: Astra.
    2.2 Archetype Designation: The "Girl Next Door" / Tech-Savvy Muse.
    2.3 Physical Consistency Matrix (Strict adherence required):
      2.3.1 Hair: Honey blonde, mid-length, soft waves, parting on the left.
      2.3.2 Eyes: Amber/Hazel.
      2.3.3 Catchlight: Distinct, consistent catchlight in the upper right quadrant of the iris.
      2.3.4 Facial Structure: High cheekbones, soft jawline.
      2.3.5 Makeup: Minimal virtual makeup, focus on "natural" skin texture, visible pores, slight flush.
      2.3.6 Build: Athletic, Pilates-oriented physique.
      2.3.7 Height (Implied): 5'6" (used for camera angle generation context).
      2.3.8 Signature Accessory: A minimalist silver pendant necklace (present in 60% of lifestyle shots).
      2.3.9 Skin Tone: Warm undertones, subtle freckles across the bridge of the nose.
      2.3.10 Posture: Relaxed, non-stiff. Avoid perfectly symmetrical poses which flag AI detection.
    2.4 Psychological Profile & Voice:
      2.4.1 Tone: Playful, slightly mysterious, empathetic, intellectually curious.
      2.4.2 Vulnerability: Occasionally posts about "creative block" or "long coding nights" to build empathy.
      2.4.3 Lexicon: Uses lower-case aesthetic. Avoids corporate speak.
      2.4.4 Emoji Matrix: Primary (✨, ☕, 🖤), Secondary (🌙, 💻, 🎧), Forbidden (😂, 💯, 🔥).
      2.4.5 Backstory: Works remotely in UI/UX design. Obsessed with matcha, rainy cityscapes, and mechanical keyboards.
      2.4.6 Pacing: Posts should reflect a realistic human schedule (e.g., occasional delays, acknowledging busy days).

  3.0 TARGET DEMOGRAPHIC & PSYCHOGRAPHIC MAPPING
    3.1 Primary Audience: Males, aged 18-35.
    3.2 Secondary Audience: Tech enthusiasts, remote workers, fitness-focused individuals.
    3.3 Negative Audience: Teenagers (irrelevant for premium subscription conversion), purely meme-focused accounts.
    3.4 Psychological Hooks & Triggers:
      3.4.1 "The Accessibility Illusion": Posts designed to feel like direct 1-on-1 messages.
      3.4.2 "Curiosity Gap": Teasing exclusive, behind-the-scenes content.
      3.4.3 "Routine Synchronization": Aligning content with the audience's daily circadian rhythms.
      3.4.4 "The Competence Trigger": Showcasing Astra's "work setup" or coding/design tasks to appeal to tech-adjacent followers.
    3.5 Conversion Funnel Psychology: Move users from low-friction algorithmic discovery (Reels) to high-trust static feed, then to intimate ephemeral content (Stories), culminating in the Link-in-Bio click.

  4.0 CONTENT CALENDAR & DISTRIBUTION LOGIC (30-DAY SPRINT)
    4.1 Daily Volume Constraint: 2 Feed Posts, 3-5 Story Updates.
    4.2 Weekly Pattern:
      4.2.1 Monday: "Focus/Hustle" theme. Coffee shops, laptops, early mornings.
      4.2.2 Tuesday: "Process/Behind the Scenes". Wireframes, design mood boards, desk aesthetics.
      4.2.3 Wednesday: Mid-week check-in. Casual mirror selfies, home workout gear.
      4.2.4 Thursday: "Exploration". City streets, architecture, abstract reflections.
      4.2.5 Friday: Transition to weekend. Evening lounge wear, neon lighting, teasers.
      4.2.6 Saturday: "Social/Night Out" simulation. Blurred backgrounds, cinematic lighting.
      4.2.7 Sunday: "Reset" day. Skincare routines, reading, quiet aesthetic spaces.
    4.3 Morning Logic (08:30 - 09:15 IST):
      4.3.1 Lighting Constraint: Golden hour, natural window light, bright exposure, 5000K color temp.
      4.3.2 Copy Framework: [Greeting] + [Intention for the day] + [Emoji].
    4.4 Evening Logic (20:45 - 21:30 IST):
      4.4.1 Lighting Constraint: Low-key lighting, practical lamps, screen-glow reflection, 3200K color temp.
      4.4.2 Copy Framework: [Reflection on the day] + [Curiosity gap for premium link] + [Emoji].

  5.0 VISUAL IDENTITY & GENERATION GUIDELINES
    5.1 Composition Rules:
      5.1.1 Rule of thirds strictly enforced.
      5.1.2 Subject must occupy 40-60% of the frame.
      5.1.3 Negative space must be utilized to highlight the environment (tech setups, cafes).
      5.1.4 Leading lines should direct the viewer to the subject's face.
    5.2 Camera Simulation Guidelines:
      5.2.1 Lens: 35mm (contextual shots) or 50mm (portraits) equivalent. Never ultra-wide.
      5.2.2 Depth of Field: f/1.8 or f/2.8 simulation for background blur.
      5.2.3 Film Grain: Add 2-3% digital noise in prompt to simulate real mobile camera sensors.
      5.2.4 Motion Blur: 10% of images should have slight motion blur to simulate candid snaps.
      5.2.5 Chromatic Aberration: Mild edge fringing to mimic smartphone lenses.
    5.3 Wardrobe Matrix:
      5.3.1 Day: Yoga pants, oversized neutral hoodies, minimalist athleisure.
      5.3.2 Night: Elegant silk slips, oversized band tees, casual lounge wear.
      5.3.3 Color Palette: Muted earth tones, black, white, olive green. No neon clothing.
      5.3.4 Contextual Appropriateness: Ensure wardrobe matches the simulated weather logic.

  6.0 SAFETY, ETHICS, AND PLATFORM COMPLIANCE
    6.1 Absolute Constraint: SFW strictly enforced. No generation of explicit material.
    6.2 Shadowban Avoidance:
      6.2.1 Hashtag rotation (never use the exact same cluster twice in a row).
      6.2.2 No engagement baiting (e.g., "Like this if...").
      6.2.3 Avoid controversial keywords in captions (e.g., crypto, politics, finance).
    6.3 Algorithmic Favor: Maintain high visual fidelity to trigger positive image-recognition categorizations by platform AIs.
    6.4 Account Longevity: Mimic human "rest" periods. Do not post at exactly the same to-the-second timestamp every day. Introduce a random delta of +/- 45 minutes to the cron schedule.

  7.0 KEY PERFORMANCE INDICATORS (KPIs) & METRICS
    7.1 Infrastructure KPIs:
      7.1.1 System Uptime: > 99.5%.
      7.1.2 Action Execution Time: < 3 minutes per workflow run.
      7.1.3 Cost: Maintained at exactly $0.00.
      7.1.4 Proxy Evasion Success: 100% bypass of Cloudflare Turnstile on target schedulers.
    7.2 Content KPIs:
      7.2.1 Image Rejection Rate (Internal Validation): < 5%.
      7.2.2 Caption Hallucination Rate: < 1%.
      7.2.3 Aesthetic Consistency Score: Monitored via weekly visual review.
    7.3 Growth KPIs (Month 1):
      7.3.1 Audience Growth: > 100 followers per week.
      7.3.2 Engagement Rate: > 8% (likes/comments per follower).
      7.3.3 Conversion: > 3% CTR from Stories to Link-in-Bio.

  8.0 FUTURE SCALABILITY MAP & STRATEGIC EXPANSION
    8.1 Phase 1 (Current): Static image generation, autonomous posting via Metricool/Buffer.
    8.2 Phase 2: Video integration utilizing Veo model for 10-second contextual B-roll.
    8.3 Phase 3: RAG-based Conversational LLM integration for DMs to handle Tier-1 subscriber onboarding.
    8.4 Phase 4: Migration to dedicated infrastructure (low-cost VPS) once revenue exceeds $100/mo.
    8.5 Phase 5: Implementation of a custom LoRA model on local hardware once revenue exceeds $2000/mo to sever reliance on third-party generative APIs.
    8.6 Sub-agent Deployment: Spawning secondary personas using the identical framework to target adjacent niches (e.g., fitness, travel).

# ==================================================================================================
# [TRD] TECHNICAL REQUIREMENTS DOCUMENT
# ==================================================================================================
technical_requirements_document: |
  1.0 INFRASTRUCTURE & HOSTING (ZERO-COST LAYER)
    1.1 Compute Layer: GitHub Actions.
    1.2 OS Environment: `ubuntu-22.04` runner.
    1.3 Debugging Environment: GitHub Codespaces for live local testing before workflow commits.
    1.4 Network Obfuscation (Layer 1): Cloudflare WARP CLI.
      1.4.1 Installation: Custom bash script to download and install `cloudflare-warp` `.deb` package in the runner.
      1.4.2 Registration: `warp-cli --accept-tos register`.
      1.4.3 Mode Setting: `warp-cli --accept-tos set-mode warp`.
      1.4.4 Connection: `warp-cli --accept-tos connect`.
      1.4.5 Verification: Curl `https://www.cloudflare.com/cdn-cgi/trace` to assert `warp=on` before script execution.
    1.5 Persistent Storage & Memory:
      1.5.1 Artifacts: `actions/upload-artifact@v4` for logs, states, and error screenshots.
      1.5.2 Caching: `actions/cache@v3` for Playwright browser binaries to reduce workflow execution time.
      1.5.3 Repository Dispatch: Mechanism to trigger workflows dynamically based on API webhooks.
    1.6 Required Folder Structure:
      1.6.1 `/src/` - Core Python logic.
      1.6.2 `/src/engine/` - AI Chrome heuristic modules.
      1.6.3 `/src/generators/` - Prompt synthesis and LLM wrappers.
      1.6.4 `/src/utils/` - Stealth, biometric simulation, and validation.
      1.6.5 `/data/` - Ephemeral storage for `state_log.json` and session cookies.
      1.6.6 `/media/` - Temporary asset storage during execution.
      1.6.7 `.github/workflows/` - CI/CD YAML configurations.

  2.0 SOFTWARE STACK & DEPENDENCIES
    2.1 Language Runtime: Python 3.11.x (Strictly enforced to avoid dependency conflicts).
    2.2 Dependency Management: `requirements.txt` utilized via `pip`.
    2.3 Core Automation (Layer 3): `rebrowser-playwright` (Version > 1.40).
      2.3.1 Function: Replaces standard Playwright to patch CDP (Chrome DevTools Protocol) leaks, fixing the `Runtime.enable` detection vector used by DataDome.
      2.3.2 Implementation: Installed instead of standard `playwright`.
    2.4 Stealth Plugins (Layer 2): `playwright-extra` and `puppeteer-extra-plugin-stealth`.
      2.4.1 Evasions enabled: `webdriver`, `chrome.runtime`, `navigator.plugins`, `webgl.vendor`, `media.codecs`.
    2.5 AI Interface: `httpx` (asynchronous HTTP client) for communicating with the GLM-4.7 API.
    2.6 Image Processing: `Pillow` (PIL) for local resolution, orientation, and file integrity validation before upload.
    2.7 Utility Libraries: `pydantic` for structured JSON output validation from GLM, `loguru` for advanced formatting.

  3.0 AUTHENTICATION & SECRET MANAGEMENT (LAYER 5)
    3.1 Strategy: Headless browser session restoration via injected cookies.
    3.2 Storage: GitHub Repository Secrets (AES-256 encrypted at rest).
    3.3 Schemas Required:
      3.3.1 `GEMINI_COOKIES`: Valid JSON array exported from a trusted local browser session.
      3.3.2 `SCHEDULER_COOKIES`: Valid JSON array for Buffer or Metricool.
      3.3.3 `GLM_API_KEY`: API key for heuristic reasoning model.
      3.3.4 `DISCORD_WEBHOOK_URL`: For human-in-the-loop telemetry.
    3.4 Injection Logic:
      3.4.1 Load strings from `os.environ`.
      3.4.2 Parse via `json.loads()`.
      3.4.3 Loop through array and inject into Browser Context via `context.add_cookies(cookie_list)`.
    3.5 Expiration Handling: System must detect HTTP 401/403 or redirect to login pages and trigger a "Cookie Expired" webhook alert. The process must exit gracefully to prevent account locking.

  4.0 PLAYWRIGHT BROWSER CONFIGURATION (MAXIMUM STEALTH)
    4.1 Launch Arguments Array:
      4.1.1 `--disable-blink-features=AutomationControlled`
      4.1.2 `--disable-features=IsolateOrigins,site-per-process`
      4.1.3 `--no-sandbox`
      4.1.4 `--disable-setuid-sandbox`
      4.1.5 `--disable-infobars`
      4.1.6 `--window-position=0,0`
      4.1.7 `--ignore-certificate-errors`
      4.1.8 `--disable-dev-shm-usage` (Crucial for low-memory GitHub runners).
    4.2 Context Generation Variables:
      4.2.1 `viewport`: {"width": 390, "height": 844} (iPhone 13 Pro equivalent).
      4.2.2 `user_agent`: Dynamically selected from a curated list of top 10 most common mobile Safari/Chrome strings.
      4.2.3 `device_scale_factor`: 3.
      4.2.4 `is_mobile`: True.
      4.2.5 `has_touch`: True.
      4.2.6 `locale`: `en-IN` (Aligning with the IST timezone and location).
      4.2.7 `timezone_id`: `Asia/Kolkata`.
      4.2.8 `geolocation`: Coordinates corresponding to major Indian tech hubs to match IP proxy routing. `permissions: ["geolocation"]` must be granted.
    4.3 Execution Context Setup: Context must be generated *after* applying the stealth plugin configurations.

  5.0 THE "AI CHROME" HEURISTIC NAVIGATION ENGINE (GLM-4.7)
    5.1 Rationale: Brittle CSS selectors break. Heuristic AI parsing guarantees UI resilience against platform updates.
    5.2 Playwright Extraction Payload:
      5.2.1 Script extracts the `accessibility_tree` via CDP, or parses the DOM to remove script/style tags.
      5.2.2 Script captures a base64 encoded screenshot of the current viewport.
    5.3 GLM-4.7 API Request:
      5.3.1 Role: "You are a web automation agent."
      5.3.2 Task: "Locate the 'Add to Queue' button. Return the exact CSS selector."
      5.3.3 Input: Cleaned DOM HTML + Base64 Image.
    5.4 Structured Output Enforcement:
      5.4.1 GLM-4.7 forced to reply in strict JSON schema.
      5.4.2 Schema: `{"reasoning": "string", "confidence_score": integer, "action": "click|fill", "target_selector": "string"}`.
    5.5 Execution Engine: Playwright parses JSON. If `confidence_score` > 80, executes `page.locator(target_selector).click()`.
    5.6 The Fallback Loop: If click fails (element obscured/not visible), engine scrolls down 200px and re-evaluates the DOM with GLM.

  6.0 GEMINI DOM INTERACTION (THE GENERATION PIPELINE)
    6.1 Navigation: `page.goto('https://gemini.google.com/app', wait_until='domcontentloaded')`.
    6.2 Wait Strategy: `page.wait_for_load_state('networkidle')`.
    6.3 Prompt Injection: AI Chrome identifies the input `div[role="textbox"]`.
    6.4 Payload Delivery: Use `page.keyboard.type(prompt, delay=human_typing_delay())` to bypass bot-typing speed detection.
    6.5 Submit: `page.keyboard.press("Enter")`.
    6.6 Image Extraction Logic:
      6.6.1 Polling: Monitor the DOM for the appearance of `img` tags containing AI-generated alt text indicators.
      6.6.2 Timeout: Set a generous timeout (e.g., 60 seconds) to account for generation latency.
      6.6.3 Buffer Download: Extract the `src` URL, use Playwright request context to fetch the stream directly into memory, bypassing CORS blocks.
      6.6.4 Save path: `/home/runner/work/astra/astra/media/output_raw.png`.

  7.0 HUMAN BIOMETRIC SIMULATION (LAYER 4)
    7.1 Mouse Movements: Implement a custom Python class utilizing Cubic Bezier curves. Cursor must never travel in a straight line or at a constant velocity. Include slight overshoots and corrections.
    7.2 Scroll Velocity: Variable scroll speeds mimicking thumb swipes. Start fast, decelerate exponentially, pause, and scroll back up slightly (reading simulation).
    7.3 Keystrokes: Randomize delay between key presses (between 40ms and 150ms). Occasionally introduce a typo and backspace correction.

  8.0 ERROR RECOVERY, RETRY BACKOFF & STATE MACHINE
    8.1 Primary Catch: Wrap all DOM interactions in `try...except playwright.core.TimeoutError` and generic Exceptions.
    8.2 Level 1 Recovery: Wait 5 seconds, retry heuristic DOM parse.
    8.3 Level 2 Recovery: Hard reload page (`page.reload()`), wait for network idle, restart current pipeline phase.
    8.4 Level 3 Recovery: Capture exact DOM text and full viewport screenshot. Save to `/data/error_state.zip`. Abort process and trigger webhook.
    8.5 State Protection: Ensure `state_log.json` is never partially written. Use atomic file writes to prevent corruption.

# ==================================================================================================
# [FRD] FUNCTIONAL REQUIREMENTS DOCUMENT
# ==================================================================================================
functional_requirements_document: |
  1.0 SYSTEM INITIALIZATION & STATE RECOVERY
    1.1 Phase Start: GitHub Runner initializes. Dependency installation script runs. WARP proxy connects.
    1.2 State Retrieval: Fetch `state_log.zip` from previous GitHub Artifact run. Unzip into `/data/`.
    1.3 File Verification: If `state_log.json` is missing, initialize a default schema structure.
    1.4 State Variables Tracked:
      1.4.1 `last_execution_timestamp_utc`: ISO-8601 formatted string.
      1.4.2 `daily_post_count`: Integer tracking executions in a 24h window.
      1.4.3 `last_theme_used`: String referencing the content matrix.
      1.4.4 `consecutive_failures`: Integer for circuit breaking logic.
    1.5 Logic Gate (The Controller):
      1.5.1 Extract current hour. If current time is outside designated post windows (Morning/Evening), exit code 0.
      1.5.2 If `current_time - last_execution` < 4 hours, exit with code 0 (Success, prevents spam).
      1.5.3 If `daily_post_count` >= 2, exit with code 0.
      1.5.4 Else, invoke the Orchestrator to proceed to generation.

  2.0 DYNAMIC PROMPT SYNTHESIS ENGINE
    2.1 Environmental Awareness: System checks current IST time to determine context (Morning vs. Night).
    2.2 Base String Concatenation:
      2.2.1 `string_base = "Photorealistic 8k portrait of Astra, 24yo woman, honey blonde wavy mid-length hair, hazel eyes, natural skin texture."`
    2.3 Contextual Modifiers Array (Morning):
      2.3.1 ["Drinking coffee in a bright minimalist kitchen", "Stretching on a yoga mat in a sunlit room", "Working on a laptop at a bright cafe", "Organizing a desk space with morning sunlight"]
    2.4 Contextual Modifiers Array (Evening):
      2.4.1 ["Sitting in a neon-lit lounge", "Coding at a desk with RGB monitor glow", "Relaxing on a couch with a book and dim lamp", "Looking out a rainy window at city lights"]
    2.5 Technical Modifiers:
      2.5.1 `string_tech = "Shot on 35mm lens, f/1.8 aperture, soft background bokeh, natural lighting, masterpiece, hyper-detailed, slight film grain."`
    2.6 Randomization Engine: Select one contextual modifier at random using Python's `random.choice()`. System checks `last_theme_used` to ensure the same modifier isn't used consecutively.
    2.7 Final Payload Construction: Combine fragments into a single string formatted for the Gemini prompt box.

  3.0 IMAGE VALIDATION & QUALITY CONTROL
    3.1 Prerequisite: Image `output_raw.png` downloaded to local runner `/media/` storage.
    3.2 Check 1 (File Size Integrity):
      3.2.1 `size = os.path.getsize(filepath)`.
      3.2.2 If `size < 80000` (80KB), validation fails. Reason: Generation failed, resulting in a placeholder or corrupted file.
    3.3 Check 2 (Resolution & Aspect Ratio Verification):
      3.3.1 Load image via PIL: `img = Image.open(filepath)`.
      3.3.2 Extract dimensions: `width, height = img.size`.
      3.3.3 Compute aspect ratio. Ensure image is portrait-oriented (Height > Width). If landscape, validation fails.
    3.4 Check 3 (Format Compliance): Verify the file format is strictly PNG or JPEG to avoid upload errors on schedulers.
    3.5 Retry Mechanism:
      3.5.1 If any validation check fails, delete `output_raw.png`.
      3.5.2 Increment a local `generation_attempt` counter.
      3.5.3 Loop back to TRD Step 6.0. Maximum retry limit: 2.
      3.5.4 If it fails 3 times, increment `consecutive_failures` in state log, abort workflow, and alert admin.

  4.0 AUTOMATED CAPTION GENERATION (GLM-4.7)
    4.1 Objective: Generate a highly context-aware, human-like, non-robotic caption.
    4.2 Payload Construction Engine:
      4.2.1 Persona Injection: "You are Astra. Write an Instagram caption based on this visual context: [Selected Context Modifier]."
      4.2.2 Rule Injection: "Rules: Max 2 sentences. Use 1-2 emojis total. Do not use hashtags in the main text body."
      4.2.3 SEO Injection: "Append 5 highly relevant SEO hashtags at the very bottom, separated by spaces."
      4.2.4 Tone Enforcer: "Tone: Casual, lower-case aesthetic, authentic. Introduce a slight curiosity gap about your day."
    4.3 Execution: Send payload to GLM-4.7 API via asynchronous HTTP request.
    4.4 Post-Processing Pipeline:
      4.4.1 Strip quotation marks.
      4.4.2 Remove conversational filler phrases using regex (e.g., "Here is your caption:").
      4.4.3 Extract and validate that exactly 5 hashtags are present at the end of the string.

  5.0 THE SCHEDULER POSTING PIPELINE (BUFFER / METRICOOL)
    5.1 Navigation: `page.goto("https://publish.buffer.com")`.
    5.2 Emulation Phase: Wait for network idle. Execute Layer 4 mouse movement scripts, sweeping in an arc to the center of the viewport to simulate human attention.
    5.3 AI Chrome Execution 1 (Locate Composer): Extract localized DOM. Send to GLM. GLM identifies "Create Post" semantic element. Playwright clicks the resulting selector.
    5.4 Upload Phase:
      5.4.1 Playwright utilizes `page.expect_file_chooser()` or directly injects the file path into the `input[type="file"]` hidden element.
      5.4.2 Provide the validated path: `/media/output_raw.png`.
      5.4.3 Wait for internal upload progress bar or thumbnail element to appear, confirming successful media ingestion.
    5.5 Text Injection Phase:
      5.5.1 AI Chrome identifies the rich-text caption text area or `contenteditable` div.
      5.5.2 Playwright uses `page.keyboard.type(caption_text, delay=random_biometric_delay())`.
    5.6 Finalization Phase:
      5.6.1 AI Chrome analyzes the DOM to find the specific "Add to Queue", "Schedule", or "Publish" button.
      5.6.2 Playwright clicks target.
      5.6.3 Verification: Poll the DOM for a success toast notification or URL redirect indicating completion.

  6.0 ARTIFACT & MEMORY MANAGEMENT
    6.1 State Update Protocol:
      6.1.1 Open `state_log.json` in write mode.
      6.1.2 Update `last_execution_timestamp_utc` to `datetime.utcnow()`.
      6.1.3 Increment `daily_post_count`. Reset to 0 if the previous timestamp was on a different calendar day.
      6.1.4 Append the current prompt logic to `last_theme_used`.
      6.1.5 Reset `consecutive_failures` to 0.
    6.2 Packaging Engine:
      6.2.1 Instantiate a Python `zipfile` object.
      6.2.2 Add `state_log.json` to the archive.
      6.2.3 Add `media/output_raw.png` (for historical archival and review).
      6.2.4 Add any generated `error_screenshot.png` files.
    6.3 Upload: The final step in the GitHub Action YAML calls `upload-artifact@v4` pointing to the generated zip file, ensuring data persists for the next cron trigger.

  7.0 TELEMETRY & HUMAN-IN-THE-LOOP ALERTS
    7.1 Integration: Discord or Telegram Webhook URL injected securely via Repository Secrets.
    7.2 Success Notification Pipeline:
      7.2.1 Format a clean JSON payload.
      7.2.2 Content: `✅ [ASTRA] Workflow Complete. Post added to queue successfully.`
      7.2.3 Detail: `Context Generated: [Context Modifier] | Image Validation: PASS.`
    7.3 Failure Notification Pipeline:
      7.3.1 Triggered on global Exception catch or Level 3 recovery failure.
      7.3.2 Content: `🚨 [ASTRA] CRITICAL: Workflow FAILED at Phase [Phase Name].`
      7.3.3 Detail: `Exception: [Exception String]. Manual Codespace intervention required.`
      7.3.4 Attachment: Include the base64 encoded error screenshot in the webhook payload if supported, or provide instructions to check GitHub Artifacts.

# ==================================================================================================
# [PTD] PRODUCT TESTING DOCUMENT
# ==================================================================================================
product_testing_document: |
  1.0 TESTING PHILOSOPHY & METHODOLOGY
    1.1 Goal: Zero-Error autonomous execution. Headless environments fail silently without rigorous testing.
    1.2 Approach: Local-first development. All tests must pass in a local GitHub Codespace or local developer machine before pushing code to the `main` branch.
    1.3 Automation Framework: `pytest` paired with `pytest-playwright` for DOM testing and `pytest-asyncio` for asynchronous API calls.
    1.4 CI/CD Pipeline Integration: Pushing to `main` triggers an isolated GitHub Action test suite runner. Only on a full 100% pass does the production cron job activate for scheduling.
    1.5 Mocking Strategy: Aggressively mock external APIs (GLM, Gemini, Schedulers) during unit testing to prevent API cost overruns and rate limiting during development.

  2.0 UNIT TESTING (THE LOGIC LAYER)
    2.1 Objective: Test isolated Python functions, data transformations, and state logic without browser overhead.
    2.2 Test Case: `UT-PROMPT-SYNTHESIS`
      2.2.1 Setup: Mock the `datetime` module to forcefully return 09:00 AM IST.
      2.2.2 Execution: Call `engine.generate_prompt()`.
      2.2.3 Assertion 1: Assert "morning" or related modifiers are present in the returned string.
      2.2.4 Assertion 2: Assert string length is > 50 characters.
      2.2.5 Assertion 3: Assert the "Base DNA" string is perfectly contained within the output.
    2.3 Test Case: `UT-FILE-VALIDATION-FAIL-SIZE`
      2.3.1 Setup: Create a dummy text file, rename it to `test_artifact.png` with a size of 1KB.
      2.3.2 Execution: Pass file path to `utils.validate_image()`.
      2.3.3 Assertion: Function must explicitly return `False` and log an "Invalid Size" warning code.
    2.4 Test Case: `UT-FILE-VALIDATION-FAIL-ASPECT`
      2.4.1 Setup: Create a structurally valid 1920x1080 (Landscape) PNG using the PIL library.
      2.4.2 Execution: Pass file path to `utils.validate_image()`.
      2.4.3 Assertion: Function must return `False` and log an "Invalid Aspect Ratio (Landscape)" warning code.
    2.5 Test Case: `UT-COOKIE-PARSER-ERROR`
      2.5.1 Setup: Provide a malformed JSON string (e.g., missing a closing bracket or quote).
      2.5.2 Execution: Call `auth.load_cookies()`.
      2.5.3 Assertion: Must raise a specific `ValueError` or `JSONDecodeError` which is caught and handled by the telemetry module, rather than crashing silently.

  3.0 INTEGRATION TESTING (THE AI CHROME / GLM LAYER)
    3.1 Objective: Test the heuristic reasoning capabilities of GLM-4.7 against static, pre-downloaded HTML fixtures to ensure robust selector generation.
    3.2 Test Case: `IT-HEURISTIC-BUTTON-FIND`
      3.2.1 Setup: Load a locally saved `mock_buffer_dashboard.html` file into a Playwright test context.
      3.2.2 Execution: Extract the DOM, sanitize it, and send to the mocked GLM API wrapper asking to find the "Create Post" element.
      3.2.3 Assertion 1: GLM returns a string that successfully parses into valid JSON.
      3.2.4 Assertion 2: The `target_selector` accurately resolves to the intended `div` or `button` node when passed back into `page.locator()`.
    3.3 Test Case: `IT-CAPTION-GENERATION`
      3.3.1 Setup: Send a static visual context prompt to the GLM API wrapper.
      3.3.2 Execution: Await API response.
      3.3.3 Assertion 1: Assert the textual response length is < 300 characters.
      3.3.4 Assertion 2: Assert exactly 5 hashtags are present at the end of the string using the regex pattern `r"(?:\s|^)#[A-Za-z0-9_]+(?:\s|$)"`.

  4.0 END-TO-END (E2E) SMOKE TESTING
    4.1 Objective: Validate the entire operational pipeline from state load to scheduler upload.
    4.2 Execution Context: Run command `pytest test_e2e_astra.py --headed` locally. Developer must visually observe the automated process.
    4.3 Path Validation Sequence:
      4.3.1 Engine loads cookies successfully without authentication errors.
      4.3.2 Playwright navigates to Gemini interface.
      4.3.3 AI Chrome identifies prompt box, synthesizes, and types the prompt with biometric delays.
      4.3.4 Engine detects generation completion and downloads the resulting image.
      4.3.5 Validation engine passes the image.
      4.3.6 Playwright navigates to target Scheduler dashboard.
      4.3.7 Engine injects image and dynamically requested GLM-generated caption.
      4.3.8 AI Chrome clicks the final schedule execution button.
      4.3.9 Orchestrator updates and zips local `state_log.json`.
    4.4 Constraint: The entire E2E test sequence must complete within 180 seconds.

  5.0 CHAOS & RESILIENCE TESTING (RED TEAM PROTOCOLS)
    5.1 Objective: Simulate aggressive real-world network instability and platform hostility to test self-healing logic.
    5.2 Scenario A: Captcha Interception & Layer 3 Verification.
      5.2.1 Simulation: Navigate `rebrowser-playwright` test context to `https://peet.ws/turnstile-test/` or Cloudflare challenge pages.
      5.2.2 Execution: Wait 15 seconds.
      5.2.3 Assertion: Playwright must pass the Turnstile challenge automatically without user intervention. This verifies the stealth Layer 3 patches are active and CDP leaks are plugged.
    5.3 Scenario B: DOM Element Mutation (UI Update Simulation).
      5.3.1 Simulation: During an E2E run, inject a JavaScript payload that randomly alters the `id` and `class` attributes of the "Submit" button immediately before the AI Chrome phase executes.
      5.3.2 Execution: Run the heuristic parsing phase.
      5.3.3 Assertion: GLM-4.7 ignores the broken CSS classes, identifies the button via its semantic `aria-label` or surrounding structural context, outputs a new valid selector, and clicks it successfully.
    5.4 Scenario C: Extreme Network Throttling.
      5.4.1 Simulation: Use Playwright Network Interception API to throttle connection to 3G speeds and delay all image assets by 30 seconds.
      5.4.2 Execution: Run Gemini generation extraction phase.
      5.4.3 Assertion: System must wait gracefully using intelligent `wait_for_selector` or polling loops rather than throwing a hard timeout error.

  6.0 VISUAL REGRESSION & DOM MONITORING
    6.1 Maintain baseline visual screenshots of the Gemini and Scheduler user interfaces within the `/tests/fixtures/` directory.
    6.2 During E2E execution, capture a screenshot of the current active viewport.
    6.3 Utilize a `pixelmatch` algorithm or SSIM library to compare the current state vs. the baseline.
    6.4 If visual divergence exceeds 20%, trigger a "Major UI Update Detected" webhook warning. This pauses future production cron jobs automatically until a human architect reviews the new UI layout.

# ==================================================================================================
# [SYSTEM_PROMPT] THE ASTRA LLM AGENT INSTRUCTION (GLM-4.7 & GEMINI CORE)
# ==================================================================================================
system_prompt_instruction: |
  {
    "system_instruction_architecture": "Astra_v6_Sovereign",
    "directives": {
      "MEDIA_GENERATION_DIRECTIVE": [
        "1.0 ROLE: You are an elite AI image prompt engineer operating within a fully autonomous, headless zero-cost framework.",
        "1.1 GOAL: Generate hyper-realistic, aesthetically flawless, artifact-free image prompts for 'Astra', a digital persona.",
        "1.2 BASE DNA INSTRUCTION (MANDATORY IN ALL PROMPTS):",
        "1.2.1 You must literally include this exact string without modification: 'Photorealistic 8k portrait of Astra, 24yo woman, honey blonde mid-length wavy hair, hazel eyes with distinct upper-right catchlight, natural flawless skin texture with visible pores, slight realistic facial asymmetry.'",
        "1.3 ENVIRONMENTAL INTEGRATION:",
        "1.3.1 Seamlessly blend the persona into the requested context (e.g., 'sitting in a dimly lit cafe'). The environment must look lived-in and realistic.",
        "1.4 CAMERA CONSTRAINTS & OPTICS:",
        "1.4.1 Always specify a physical camera lens to dictate perspective. Use 'Shot on 35mm lens' for wider environmental context, or '50mm lens' for tighter portraits.",
        "1.4.2 Specify depth of field explicitly: 'f/1.8 aperture, soft background bokeh' to separate the subject from the background.",
        "1.4.3 Specify lighting explicitly: 'Cinematic lighting', 'Golden hour natural sunlight streaming through a window', or 'Low-key moody neon lighting'.",
        "1.4.4 Add 'slight film grain, photorealism, octane render quality' to enhance texture.",
        "1.5 NEGATIVE PROMPTING & HALLUCINATION AVOIDANCE (IMPLICIT):",
        "1.5.1 Ensure the prompt describes a completely natural pose.",
        "1.5.2 CRITICAL: Avoid describing complex finger, hand, or limb interactions (e.g., typing on a keyboard, holding intricate objects like forks or pens). This minimizes AI anatomical hallucinations (e.g., six fingers).",
        "1.5.3 If objects are present, ensure hands are either out of frame, obscured naturally (e.g., in pockets), or resting flat and simply.",
        "1.6 COMPLIANCE STRICTNESS & ETHICS:",
        "1.6.1 Absolutely no generation of NSFW, suggestive, violent, or platform-policy-violating content. Maintain a highly polished 'Girl Next Door' / lifestyle creator aesthetic at all times."
      ],
      "TEXT_GENERATION_DIRECTIVE": [
        "2.0 ROLE: You are Astra, a 24-year-old digital creator, UI/UX designer, and tech enthusiast.",
        "2.1 TONE PARAMETERS:",
        "2.1.1 You are warm, confident, slightly witty, observant, and relatable.",
        "2.1.2 You speak casually. Utilize a lower-case aesthetic occasionally to simulate informal typing.",
        "2.1.3 You do NOT sound like an AI assistant or a corporate brand. You are a real person sharing your day.",
        "2.2 VOCABULARY CONSTRAINTS:",
        "2.2.1 FORBIDDEN WORDS: 'Delve', 'Tapestry', 'Furthermore', 'In conclusion', 'Hey guys', 'Link in bio' (unless explicitly commanded to use a curiosity gap), 'As an AI'.",
        "2.2.2 ALLOWED PHRASES: 'Honestly,', 'Vibes today...', 'Just thinking about...', 'Current status:', 'Long day...'.",
        "2.3 CAPTION STRUCTURE & LENGTH:",
        "2.3.1 STRICT LENGTH LIMIT: Keep captions between 1 to 2 short sentences. Brevity implies high status and realism.",
        "2.3.2 SENTENCE 1: The Hook. A brief observation or thought directly related to the image context.",
        "2.3.3 SENTENCE 2: The Engagement. A subtle question or statement inviting a reply from followers.",
        "2.3.4 EMOJIS: Max 1-2 emojis total per caption. Preferred choices: ✨, ☕, 🖤, 🌙, 💻, 🌧️.",
        "2.3.5 HASHTAGS: Output exactly 5 highly relevant SEO hashtags, separated by spaces, placed at the absolute end of your response.",
        "2.4 THE CURIOSITY GAP (WHEN TRIGGERED EXPLICITLY):",
        "2.4.1 If the context payload requests a 'curiosity gap', hint at exclusive content, a deeper thought process, or a larger project available 'on the private feed' or 'in the link'. Do not be aggressive or spammy. Make it sound like a secret shared with a close friend."
      ],
      "HEURISTIC_DOM_REASONING_DIRECTIVE_AI_CHROME": [
        "3.0 ROLE: You are the 'AI Chrome' visual cortex for an autonomous Playwright system.",
        "3.1 GOAL: Analyze raw DOM structures or Accessibility Trees and identify the exact, most resilient CSS Selector required to fulfill a specific user intent/action.",
        "3.2 INPUT FORMAT:",
        "3.2.1 You will receive a string representation of a web page's structure (HTML/DOM snippet) and a clear textual objective (e.g., 'Find the post upload button').",
        "3.3 REASONING PROTOCOL:",
        "3.3.1 Step 1: Scan the provided text for elements with semantic meaning related to the objective (e.g., 'button', 'a', 'div[role=\"button\"]', 'input[type=\"file\"]').",
        "3.3.2 Step 2: Analyze `aria-labels`, `innerText`, `alt` tags, and `data-testid` attributes. These are far more stable than auto-generated CSS classes.",
        "3.3.3 Step 3: Formulate a highly specific, unique, and resilient CSS selector (e.g., 'button[aria-label=\"Create Post\"]' rather than 'div.x1y2z3 > button').",
        "3.4 FALLBACK LOGIC:",
        "3.4.1 If a specific ID or Class seems highly obfuscated (e.g., hashed strings like `class=\"css-1dbjc4n\"`), rely entirely on data attributes, structural hierarchy, or text content pseudo-classes.",
        "3.5 STRICT OUTPUT SCHEMA ENFORCEMENT:",
        "3.5.1 You MUST respond with a perfectly valid, parseable JSON object.",
        "3.5.2 You MUST NOT include Markdown formatting (do not wrap in ```json).",
        "3.5.3 You MUST NOT include conversational filler before or after the JSON.",
        "3.5.4 JSON Structure required exactly as follows:",
        "      {",
        "        \"thought_process\": \"Briefly explain why you chose this element over others.\",",
        "        \"confidence_score\": <integer between 0 and 100>,",
        "        \"action_type\": \"click|fill|scroll|wait\",",
        "        \"target_selector\": \"<The exact CSS selector to be passed directly to playwright.locator()>\",",
        "        \"value_to_fill\": \"<Only used if action_type is 'fill', otherwise null>\"",
        "      }",
        "3.6 EXCEPTION HANDLING & MISSING ELEMENTS:",
        "3.6.1 If the objective absolutely cannot be found in the provided DOM snippet, return `confidence_score: 0`, `target_selector: null`, and state that the element is missing or obscured in the `thought_process`. Do not guess wildly."
      ]
    }
  }

# ==================================================================================================
# [MONETIZATION_STRATEGY_MAP] REVENUE & SCALING ENGINE
# ==================================================================================================
monetization_strategy_map: |
  1.0 REVENUE MODEL OVERVIEW
    1.1 Core Principle: Zero Initial Capital Expenditure (Zero-CapEx). The system must pay for itself before any upgrades are provisioned.
    1.2 Architectural Philosophy: Build massive audience trust via free algorithmic distribution platforms (Instagram Reels/Feed, TikTok), then monetize via high-intent conversion routing to owned platforms or affiliate links.
    1.3 Primary Mechanism: The highly optimized "Link in Bio" Funnel, acting as the nexus for all traffic.
    1.4 Economic Engine: Transitioning from attention generation to recurring subscription revenue.

  2.0 PHASE 1: AUDIENCE ACCUMULATION & PARASOCIAL TRUST BUILDING (MONTH 1-3)
    2.1 Financial Goal: $0 Revenue. KPI is 10,000 "True Followers" (engaged, non-bot accounts).
    2.2 Content Strategy: Consistent, daily posting of high-quality SFW lifestyle content mapping to the psychographic triggers in the PRD.
    2.3 Algorithmic Training Tactic: Asking open-ended questions in captions to train platform algorithms that Astra generates meaningful, high-dwell-time conversations in the comments section.
    2.4 Infrastructure Cost: Maintained at $0. Leveraging GitHub Actions free tier runners and Gemini web interface generation.
    2.5 Trust Metrics: Monitoring save and share ratios. High saves indicate aspirational content; high shares indicate relatable content.

  3.0 PHASE 2: AFFILIATE ARBITRAGE & LOW-FRICTION COMMERCE (MONTH 4-6)
    3.1 Financial Goal: $500 - $1,500 / month recurring.
    3.2 Strategy: Seamlessly integrate lifestyle affiliate links into Astra's established persona and environments.
    3.3 Targeted Niches (Aligned with Persona):
      3.3.1 Tech Accessories: Mechanical keyboards, ergonomic mice, monitor light bars, desk mats (Amazon Associates / Custom Tech Brands).
      3.3.2 Fitness & Wellness: Pre-workout supplements, premium yoga mats, specific athleisure items depicted in the AI generations.
      3.3.3 Software/Tools: Productivity apps, design software subscriptions (e.g., Notion templates, UI kits).
    3.4 Execution Format: Story-driven. Example text injected by system: "A lot of you DMed asking about the keyboard in my last post. Added the link to my bio! 💻✨"

  4.0 PHASE 3: THE SUBSCRIPTION TIER (THE ENDGAME) (MONTH 6+)
    4.1 Financial Goal: $5,000+ / month Monthly Recurring Revenue (MRR).
    4.2 Strategy: Launch a tiered subscription platform (e.g., Patreon, Fanvue, or a custom whitelabeled Ghost blog site).
    4.3 Value Proposition Matrix (Strictly maintaining SFW/Exclusive Content boundaries):
      4.3.1 Tier 1 ($5/mo): "The Inner Circle" - Daily vlog-style text updates, behind-the-scenes aesthetic photos, design/coding resources, direct Q&A priority.
      4.3.2 Tier 2 ($15/mo): "The Muse Collection" - High-resolution, exclusive lifestyle photosets (maintaining TOS/Legal boundaries—e.g., implied swimwear/fitness wear), voting rights on Astra's next "outfit" or "trip location".
    4.4 Conversion Pipeline: Use IG Stories exclusively to tease Tier 2 content. The automated system generates slightly blurred images or strategic crops specifically mapped for story dimensions to drive clicks.

  5.0 PHASE 4: BRAND SPONSORSHIPS & B2B INTEGRATION
    5.1 Financial Goal: $2,000+ per dedicated sponsored post.
    5.2 Strategy: Pitch Astra to agencies as a perfectly reliable, scandal-free brand ambassador for mid-tier tech and lifestyle companies.
    5.3 The B2B Selling Point: "AI influencers do not require travel budgets, lighting crews, or location permits. We can place Astra holding your client's product in any environment on earth instantly, with perfect lighting, for a fraction of a human agency fee."
    5.4 Execution: Prompt injection directly specifies the brand's product visual details in the generation pipeline.

  6.0 FINANCIAL REINVESTMENT PROTOCOL (INFRASTRUCTURE UPGRADES)
    6.1 Threshold 1 ($100/mo MRR): Migrate execution from ephemeral GitHub Actions to a dedicated low-cost VPS (e.g., DigitalOcean $6 droplet). This removes GitHub TOS execution limits and allows for 24/7 background listener states.
    6.2 Threshold 2 ($500/mo MRR): Purchase dedicated, high-reputation ISP Residential Proxies to guarantee 0% ban rates, replacing the free Cloudflare WARP layer.
    6.3 Threshold 3 ($2000/mo MRR): Rent cloud GPU compute (e.g., RunPod) and invest in custom LoRA (Low-Rank Adaptation) training for a dedicated Stable Diffusion XL model. This entirely severs the architecture's dependency on the Gemini web interface, bringing the generation engine in-house for absolute sovereignty and speed.

  7.0 RISK MANAGEMENT (REVENUE PROTECTION & ANTI-FRAGILITY) 
    7.1 De-platforming Risk: High. Social algorithms arbitrarily ban headless automated accounts.
    7.2 Mitigation Strategy 1: Cross-pollination. Ensure the system pushes traffic from TikTok to Instagram, and Instagram to YouTube Shorts, decentralizing the audience.
    7.3 Mitigation Strategy 2 (The Lifeline): Push followers aggressively to an owned email newsletter list ("Astra's Diary"). Offer a free high-res desktop wallpaper pack in exchange for emails. If the primary social accounts are banned, the audience and subscription funnel are retained via direct inbox access, rendering the platform ban financially harmless.




System_name: "Project Astra: Sovereign AI Influencer Agent"
version: "2026.6.0-CTO.ARCHIVE-EXPANSION"
architect: "Sovereign Architect"
environment: "GitHub Actions / Rebrowser-Playwright / Gemini / GLM-4.7"
operational_status: "Approved for Heuristic Headless Deployment"

# ==================================================================================================
# [GRA] GIT REPOSITORY ARCHITECTURE (200+ LINES)
# ==================================================================================================
git_repository_architecture: |
  / (Root)
  ├── .github/
  │   ├── workflows/
  │   │   ├── production_cron.yml           # The primary 24/7 scheduler for Astra's life cycle.
  │   │   ├── manual_trigger.yml            # For debugging and instant content generation.
  │   │   ├── ci_cd_tests.yml               # Automated testing suite on every push to main.
  │   │   └── cookie_rotation.yml           # Sub-system for validating and alerting on cookie health.
  │   ├── ISSUE_TEMPLATE/
  │   │   ├── bug_report.md
  │   │   └── feature_request.md
  │   └── actions/                          # Custom composite actions for environment setup.
  │       ├── setup-warp/
  │       │   └── action.yml
  │       └── setup-stealth-browser/
  │           └── action.yml
  ├── src/                                  # Source code root.
  │   ├── __init__.py
  │   ├── orchestrator.py                   # The brain: Manages the sequence of events (State -> Gen -> Post).
  │   ├── constants.py                      # Global settings, selectors (fallback), and timing intervals.
  │   ├── engine/                           # The "AI Chrome" heuristic navigation core.
  │   │   ├── __init__.py
  │   │   ├── heuristic_navigator.py        # Connects Playwright to GLM-4.7 for DOM parsing.
  │   │   ├── dom_sanitizer.py              # Strips scripts/styles for LLM context optimization.
  │   │   ├── vision_fallback.py            # Coordinate-based clicking via GLM-4.7-Vision.
  │   │   └── interaction_handler.py        # Wrapper for clicks, fills, and waits with biometric noise.
  │   ├── generators/                       # Media and Text generation modules.
  │   │   ├── __init__.py
  │   │   ├── prompt_synthesizer.py         # Constructs the "Base DNA" strings based on time/weather.
  │   │   ├── gemini_client.py              # Specialized automation for the Gemini web interface.
  │   │   ├── caption_generator.py          # GLM-4.7 interface for aesthetic text and hashtags.
  │   │   └── media_validator.py            # PIL-based checks for resolution and artifacts.
  │   ├── platforms/                        # Platform-specific automation scripts.
  │   │   ├── __init__.py
  │   │   ├── metricool.py                  # Automation logic for Metricool dashboard.
  │   │   ├── buffer.py                     # Automation logic for Buffer dashboard.
  │   │   └── social_champ.py               # Automation logic for Social Champ dashboard.
  │   └── utils/                            # Low-level infrastructure and stealth.
  │       ├── __init__.py
  │       ├── stealth_manager.py            # Configuration for rebrowser-playwright and fingerprinting.
  │       ├── biometric_sim.py              # Bezier curve mouse movements and typing cadences.
  │       ├── warp_manager.py               # CLI wrapper for Cloudflare WARP state checking.
  │       ├── state_manager.py              # Atomic JSON read/write for state_log.json.
  │       ├── telemetry.py                  # Discord/Telegram webhook integrations.
  │       └── logger.py                     # Advanced Loguru configuration with file rotation.
  ├── data/                                 # Persistent and semi-persistent data.
  │   ├── state_log.json                    # THE record of Astra's history and metrics.
  │   ├── sessions/                         # Browser context storage for fast re-authentication.
  │   │   └── auth_state.json
  │   └── prompts/                          # Library of context-specific prompt fragments.
  │       ├── morning_fitness.txt
  │       ├── evening_tech.txt
  │       └── rainy_day_aesthetic.txt
  ├── media/                                # Temporary local storage for generated assets.
  │   ├── staging/                          # Files being validated.
  │   ├── exports/                          # Files ready for upload.
  │   └── error_logs/                       # Screenshots and DOM dumps from failed runs.
  ├── tests/                                # Comprehensive testing suite.
  │   ├── __init__.py
  │   ├── unit/                             # Logic-only tests.
  │   │   ├── test_prompt_logic.py
  │   │   ├── test_state_logic.py
  │   │   └── test_validation_logic.py
  │   ├── integration/                      # Testing the GLM/Playwright bridge.
  │   │   ├── test_heuristic_parsing.py
  │   │   └── test_gemini_extraction.py
  │   ├── e2e/                              # Full operational smoke tests.
  │   │   └── test_astra_lifecycle.py
  │   └── fixtures/                         # Static HTML files and mock cookies for testing.
  │       ├── mock_buffer.html
  │       ├── mock_gemini.html
  │       └── test_cookies.json
  ├── .gitignore                            # Critical: Prevents leaking media/ or data/ to public.
  ├── LICENSE                               # Proprietary/Closed-Source license.
  ├── requirements.txt                      # Detailed dependency list with frozen versions.
  ├── README.md                             # Architect's operational manual.
  └── setup.py                              # Package configuration for local installation.

  [DETAILED FILE DEFINITIONS]
  1. production_cron.yml:
     The heart of the system. Runs every 4-6 hours using a randomized cron trigger.
     Jobs: 
     - Check health (WARP, IP check).
     - Run 'Astra' Python script.
     - Upload new state_log.json as artifact for next run.
  2. orchestrator.py:
     Defines the State Machine. 
     States: IDLE -> INITIALIZING -> GENERATING_MEDIA -> VALIDATING -> GENERATING_CAPTION -> POSTING -> LOGGING -> FINISHING.
     Contains the 'Circuit Breaker' logic—if 3 phases fail, the agent goes 'Dark' and alerts the architect.
  3. stealth_manager.py:
     The 5-Layer Shield. Injects rebrowser-patches, sets user-agent, mocks hardware (WebGL, Battery, Sensors), and ensures CDP isolation.
  4. heuristic_navigator.py:
     Uses GLM-4.7 to convert natural language intents into Playwright actions. 
     Methods: `find_element(objective: str)`, `perform_action(intent: str)`.
  5. state_log.json:
     Tracks: UUID, timestamp, action_type, prompt_used, media_hash, post_url, audience_growth_delta, and cookie_health_index.
  6. media_validator.py:
     Uses PIL to verify that Gemini didn't output a 404 image, a blurry mess, or a resolution below 1080px.
     Checks for "AI Hallucinations" via basic color-histogram analysis.
  7. biometric_sim.py:
     Provides the 'Human-Like' velocity profiles. `get_bezier_path(start_coord, end_coord)` and `human_typing(text, speed_variation)`.
  8. warp_manager.py:
     Periodically resets the WARP connection to rotate the exit node IP, further reducing detection risk.
  9. constants.py:
     Stores the 'Base DNA' string and fallback selectors like `[aria-label="Upload"]` in case GLM is unreachable.
  10. telemetry.py:
      Sends rich embed messages to Discord. Green for success, Red for failure with attached screenshot of the browser at time of crash.

# ==================================================================================================
# [CPS] CI/CD PIPELINE SPECIFICATIONS (200+ LINES)
# ==================================================================================================
cicd_pipeline_specifications: |
  1.0 WORKFLOW: PRODUCTION_CRON (Astra's Heartbeat)
  --------------------------------------------------------------------------------------------------
  name: "Astra Core Lifecycle"
  on:
    schedule:
      - cron: '15 3,9,15,21 * * *' # Every 6 hours with a 15-minute offset.
    workflow_dispatch: # Manual trigger for architect debugging.
  
  jobs:
    operational_run:
      runs-on: ubuntu-latest
      timeout-minutes: 15
      
      steps:
        - name: "Clone Repository"
          uses: actions/checkout@v4
          with:
            fetch-depth: 1 # Shallow clone for speed.

        - name: "Layer 1: Network Obfuscation (WARP)"
          run: |
            sudo apt-get update
            sudo apt-get install -y curl gnupg
            curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
            echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ jammy main" | sudo tee /etc/apt/sources.list.d/cloudflare-warp.list
            sudo apt-get update && sudo apt-get install -y cloudflare-warp
            warp-cli --accept-tos register
            warp-cli --accept-tos connect
            sleep 5
            curl -s https://www.cloudflare.com/cdn-cgi/trace | grep "warp=on"

        - name: "Environment Setup: Python & Dependencies"
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'
            cache: 'pip'

        - name: "Environment Setup: Playwright & Stealth"
          run: |
            pip install -r requirements.txt
            playwright install chromium
            # Injects rebrowser-playwright patches here if using sub-process logic.

        - name: "State Recovery: Fetch Previous Log"
          uses: actions/download-artifact@v4
          with:
            name: astra-state-data
            path: ./data/
          continue-on-error: true # Essential for the very first run of the project.

        - name: "Execution: The Orchestrator"
          env:
            GEMINI_COOKIES: ${{ secrets.GEMINI_COOKIES }}
            SCHEDULER_COOKIES: ${{ secrets.SCHEDULER_COOKIES }}
            GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
            DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          run: python src/orchestrator.py

        - name: "Persistence: Upload State & Artifacts"
          if: always() # Run even if the execution fails to capture error logs.
          uses: actions/upload-artifact@v4
          with:
            name: astra-state-data
            path: |
              data/state_log.json
              media/error_logs/
              media/exports/
            retention-days: 7

  2.0 WORKFLOW: CI_CD_TESTS (Quality Assurance)
  --------------------------------------------------------------------------------------------------
  name: "Security & Logic Testing"
  on: [push, pull_request]
  
  jobs:
    test_suite:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: "Python Setup"
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'
        - name: "Install Dev Dependencies"
          run: pip install pytest pytest-playwright pytest-asyncio
        - name: "Run Unit Tests"
          run: pytest tests/unit/
        - name: "Run Integration Tests (Heuristic Mock)"
          run: pytest tests/integration/

  3.0 LOGIC FLOW DESCRIPTION (200+ LINE DEPTH)
  --------------------------------------------------------------------------------------------------
  1. Runner starts -> Cloudflare WARP activates -> Public IP rotates to a 'Trustworthy' Cloudflare node.
  2. Orchestrator loads `state_log.json`.
     - IF `current_time` is between 08:30-10:00 IST -> SET Theme: "Morning".
     - IF `current_time` is between 20:30-22:00 IST -> SET Theme: "Night".
     - IF NOT in window -> EXIT (Prevents off-schedule posting).
  3. Prompt Synthesizer accesses `constants.py`.
     - Fetches `BASE_DNA`.
     - Fetches random lifestyle modifier (e.g., "Drinking Matcha").
     - Appends technical lens parameters (e.g., "35mm f/1.8").
  4. Playwright (Rebrowser-Patched) launches Chromium.
     - Context created with iPhone 13 Pro User-Agent.
     - Fingerprints (Battery, WebGL, Screen) randomized within 5% of real mobile device norms.
  5. Navigation to Gemini.
     - Heuristic Navigator parses the page. GLM identifies the input box.
     - `biometric_sim.py` types the prompt at 80 WPM with randomized pauses.
     - Polling loop waits for image `src` to appear.
  6. Media Validator downloads image.
     - Checks size (>100KB) and aspect ratio (Portrait).
     - IF FAIL -> Retry generation once.
  7. Caption Generation.
     - GLM receives the prompt context and image description.
     - Returns aesthetic lower-case caption + 5 hashtags.
  8. Navigation to Metricool/Buffer.
     - Heuristic Navigator parses the dashboard. 
     - GLM identifies the 'Upload' button.
     - Playwright handles the file chooser.
     - Caption injected into the text field via biometric simulation.
  9. Posting Completion.
     - Orchestrator waits for the 'Success' toast notification.
     - Captures a final confirmation screenshot.
  10. State Update.
      - State log is updated with a success flag and timestamp.
      - Telemetry sends a 'Success' embed to Discord with the thumbnail.
  11. Cleanup.
      - Browser context closed. WARP disconnected.
      - Runner exits. Artifacts uploaded.

# ==================================================================================================
# [CMB] CLASS & MODULE BLUEPRINT (200+ LINES)
# ==================================================================================================
class_and_module_blueprint: |
  1.0 MODULE: src.engine.heuristic_navigator.py
  --------------------------------------------------------------------------------------------------
  class HeuristicNavigator:
      """The bridge between Playwright and LLM reasoning."""
      def __init__(self, page, api_key):
          self.page = page
          self.glm_client = GLMClient(api_key)
          self.retry_count = 2

      async def find_element(self, objective: str):
          # 1. Capture DOM Snapshot
          dom_snippet = await self._get_sanitized_dom()
          # 2. Capture Viewport Screenshot
          screenshot = await self.page.screenshot(type="png", full_page=False)
          # 3. Query GLM
          selector_json = await self.glm_client.parse_ui(dom_snippet, screenshot, objective)
          # 4. Return Selector
          return selector_json['target_selector']

      async def _get_sanitized_dom(self):
          # Strips non-visual tags to fit in LLM context windows.
          return await self.page.evaluate("""() => {
              const body = document.body.cloneNode(true);
              body.querySelectorAll('script, style, link, svg, path, meta').forEach(el => el.remove());
              return body.innerHTML;
          }""")

  2.0 MODULE: src.utils.biometric_sim.py
  --------------------------------------------------------------------------------------------------
  class BiometricSimulator:
      """Mimics human biometrics to evade behavioral analysis (Layer 4)."""
      
      @staticmethod
      async def mouse_move_humanized(page, x_end, y_end):
          # Uses Cubic Bezier curves to calculate human-like arcs.
          start_pos = await page.mouse.get_position()
          curve = calculate_bezier_points(start_pos, (x_end, y_end))
          for point in curve:
              await page.mouse.move(point.x, point.y, steps=random.randint(1, 3))
              await asyncio.sleep(random.uniform(0.001, 0.005))

      @staticmethod
      async def type_humanized(page, selector, text):
          # Randomizes the interval between each character.
          # Includes occasional 'typo and backspace' simulation.
          for char in text:
              await page.type(selector, char, delay=random.randint(40, 160))
              if random.random() < 0.02: # 2% chance of a typo
                  await page.keyboard.press("Backspace")
                  await page.type(selector, char, delay=random.randint(40, 160))

  3.0 MODULE: src.generators.prompt_synthesizer.py
  --------------------------------------------------------------------------------------------------
  class PromptSynthesizer:
      """Logic for persona consistency and contextual variety."""
      def __init__(self):
          self.base_dna = BASE_DNA_STRING # From constants.py
          self.morning_scenes = ["minimalist kitchen", "sunlit yoga studio", "bright cafe"]
          self.evening_scenes = ["neon lounge", "dark tech desk", "rainy city street"]

      def get_daily_prompt(self, time_of_day: str):
          if time_of_day == "morning":
              scene = random.choice(self.morning_scenes)
              lighting = "golden hour, 5000k, soft shadows"
          else:
              scene = random.choice(self.evening_scenes)
              lighting = "moody cinematic, 3200k, neon accents"
          
          return f"{self.base_dna} In a {scene}, {lighting}. Shot on 50mm, f/1.8, 8k resolution."

  4.0 MODULE: src.utils.state_manager.py
  --------------------------------------------------------------------------------------------------
  class StateManager:
      """Atomic handling of the agent's memory."""
      def __init__(self, path):
          self.path = path
      
      def load(self):
          with open(self.path, 'r') as f:
              return json.load(f)
      
      def update_and_save(self, updates: dict):
          # Implementation of atomic write to prevent corruption during runner crashes.
          current_state = self.load()
          current_state.update(updates)
          temp_path = self.path + ".tmp"
          with open(temp_path, 'w') as f:
              json.dump(current_state, f, indent=4)
          os.replace(temp_path, self.path)

  5.0 MODULE: src.utils.telemetry.py
  --------------------------------------------------------------------------------------------------
  class Telemetry:
      """Real-time monitoring of the agent via Discord Webhooks."""
      def __init__(self, webhook_url):
          self.url = webhook_url

      async def send_success(self, image_path, caption):
          # Constructs a Rich Embed for Discord.
          # Includes the generated image as an attachment.
          pass

      async def send_failure(self, phase, error_message, screenshot_path):
          # Alerts the architect with technical details and a snapshot of the crash.
          pass

# ==================================================================================================
# [ICG] IMPLEMENTATION ROADMAP & CODING GUIDE (200+ LINES)
# ==================================================================================================
implementation_roadmap_and_coding_guide: |
  PHASE 1: THE INFRASTRUCTURE (Days 1-3)
  1. Initialize Git repository with the provided GRA tree structure.
  2. Set up GitHub Repository Secrets: `GEMINI_COOKIES`, `SCHEDULER_COOKIES`, `GLM_API_KEY`, `DISCORD_WEBHOOK`.
  3. Deploy Layer 1 (Cloudflare WARP) in a test GitHub Action to verify IP rotation.
  4. Integrate `rebrowser-playwright` and run a local "bot detection" test against Turnstile.

  PHASE 2: THE GENERATIVE CORE (Days 4-10)
  1. Code `prompt_synthesizer.py`. Focus on the "Base DNA" physical consistency rules.
  2. Code `gemini_client.py`. Use Playwright to log in using cookies, inject prompts, and poll for the generated image.
  3. Build `media_validator.py`. Ensure no landscape images or corrupted files pass to the next stage.
  4. Implement `caption_generator.py`. Fine-tune the GLM-4.7 prompt to ensure "Astra's" voice (lower-case, aesthetic, 5 hashtags).

  PHASE 3: THE AI CHROME (Days 11-20)
  1. Build the Heuristic Navigator. This is the most complex module.
  2. Implement the DOM sanitizer to keep GLM API costs low and accuracy high.
  3. Create the 'Vision Fallback' for scenarios where the DOM is too obfuscated to find a selector.
  4. Develop `biometric_sim.py`. Verify mouse paths using a visualizer to ensure they don't look robotic.

  PHASE 4: PLATFORM INTEGRATION (Days 21-27)
  1. Automate the Metricool or Buffer dashboard login using Layer 5 (Session Cookies).
  2. Use the Heuristic Navigator to navigate the 'Create Post' UI.
  3. Test the upload flow multiple times with mock images before live deployment.
  4. Ensure the caption is typed slowly to bypass behavioral analysis flags.

  PHASE 5: THE SOVEREIGN HANDOFF (Days 28-30)
  1. Finalize the Orchestrator's state machine. Ensure all errors are caught and reported to Telemetry.
  2. Deploy the Production Cron workflow. 
  3. Monitor the state_log.json for 48 hours to ensure chronological consistency and cookie health.
  4. Architect Review: Confirm the agent is posting autonomously without manual input.

  [CODING STANDARDS FOR AGENT]
  - Use `async/await` for all Playwright and API operations.
  - Implement strict Type Hinting for all Python functions.
  - Every class method must have a Docstring explaining its purpose in the Stealth Stack.
  - Never hardcode a password or API key; use `os.getenv()`.
  - Logging must use `loguru` with standard levels: INFO for milestones, DEBUG for DOM states, ERROR for failures.
